import React from "react";
import { Pagination } from "@mantine/core";

const PQuizPagination = () => {
  return (
    <div>
      <Pagination total={20} />
    </div>
  );
};

export default PQuizPagination;
