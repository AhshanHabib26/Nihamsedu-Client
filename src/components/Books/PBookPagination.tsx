import React from "react";
import { Pagination } from "@mantine/core";

const PBookPagination = () => {
  return (
    <div>
      <Pagination total={20} />
    </div>
  );
};

export default PBookPagination;
